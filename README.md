# Simple scripts for task automation

## Scripts
- __compress-pdf.sh__

    Very simple scripts that automates pdf compression reducing their size with `screen` quality on pages.
    
    usage:

    ```
    ./compress-pdf.sh
    # follow the instructions
    ```
